<Page
    x:Class="MHDB.Views.Data"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MHDB.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <!--<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        --><!--<StackPanel>
            <Image Visibility="Collapsed" Name="image" Source="https://upload.wikimedia.org/wikipedia/commons/6/61/M9-pistolet.jpg"/>
            <Button Name="TestButton" Content="Types" Click="TestButton_Click"/>
            <TextBlock Name="Test" TextWrapping="WrapWholeWords" />
        </StackPanel>--><!--
    </Grid>-->
    <ScrollViewer>
        <StackPanel>
            <Image Visibility="Collapsed" Name="image" Source="https://upload.wikimedia.org/wikipedia/commons/6/61/M9-pistolet.jpg"/>
            <Button Name="TestButton" Content="Types" Click="TestButton_Click"/>
            <Button Name="DeleteButton" Content="Delete" Click="DeleteButton_Click"/>
            <!--<TextBlock Name="Test" TextWrapping="WrapWholeWords" />-->
            <StackPanel Orientation="Horizontal">
                <ListView Name="Images" Width="200">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Image Source="{Binding Image}" Width="100" Height="100"/>
                            <!--Width="50" Height="50" Stretch="UniformToFill"-->
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <ListView Name="Test" Width="200">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Height="100" Text="{Binding ModelName}"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
